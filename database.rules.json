{
  "rules": {
    ".read": false,
    ".write": false,
    "products": {
      ".read": true,
      ".write": "auth != null && auth.token.email != null && (root.child('settings/adminEmails').child(auth.token.email.replace('.', ',')).exists() || !root.child('settings/adminEmails').exists() || (root.child('settings/ownerEmail').exists() && auth.token.email == root.child('settings/ownerEmail').val()))",
      ".indexOn": ["category", "visible", "createdAt", "price"]
    },
    "coupons": {
      ".read": true,
      ".write": "auth != null && auth.token.email != null && (root.child('settings/adminEmails').child(auth.token.email.replace('.', ',')).exists() || !root.child('settings/adminEmails').exists() || (root.child('settings/ownerEmail').exists() && auth.token.email == root.child('settings/ownerEmail').val()))",
      ".indexOn": ["active", "type", "createdAt"]
    },
    "settings": {
      ".read": true,
      ".write": "auth != null && auth.token.email != null && (root.child('settings/adminEmails').child(auth.token.email.replace('.', ',')).exists() || !root.child('settings/adminEmails').exists() || (root.child('settings/ownerEmail').exists() && auth.token.email == root.child('settings/ownerEmail').val()))"
    },
    "supportTickets": {
      ".read": true,
      ".write": true,
      ".indexOn": ["ticketId", "contact", "status", "createdAt", "updatedAt"]
    },
    "orders": {
      ".read": true,
      ".write": true,
      ".indexOn": ["userId", "phone", "email", "status", "timestamp", "orderId"]
    },
    "users": {
      "$uid": {
        ".read": true,
        ".write": true,
        "orders": {
          ".indexOn": ["status", "timestamp"]
        }
      }
    }
  }
}